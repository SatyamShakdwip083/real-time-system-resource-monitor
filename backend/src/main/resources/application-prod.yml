# Production profile: restrict CORS via CORS_ALLOWED_ORIGINS env
# Do not set app.cors.allowed-origins here; use env CORS_ALLOWED_ORIGINS
logging:
  level:
    root: WARN
    com.systemmonitor: INFO

management:
  endpoint:
    health:
      show-details: when-authorized
