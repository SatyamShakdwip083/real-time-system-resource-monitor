# Copy to .env or set in shell / deployment. Optional overrides for application.yml.
# Server
SERVER_PORT=8081

# CORS: comma-separated origins. Use * for dev only.
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# LibreHardwareMonitor remote server URL (optional)
LHM_URL=http://localhost:8085

# Spring profile (optional): dev | prod
# SPRING_PROFILES_ACTIVE=dev
