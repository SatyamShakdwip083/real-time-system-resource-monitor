# Copy to .env and adjust. Used by Vite at build time.
# Backend WebSocket and API base URL (same origin in dev via proxy).
VITE_WS_URL=
VITE_API_BASE=

# Examples (uncomment and set for production build):
# VITE_WS_URL=https://api.example.com/ws
# VITE_API_BASE=https://api.example.com
